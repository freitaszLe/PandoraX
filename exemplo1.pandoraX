// soma.pandoraX (Versão corrigida)

// Inicialize as variáveis com um valor padrão para a declaração ser válida.
inter num1 = 0
inter num2 = 0
inter resultado = 0

pandora.expose(<Calculadora de Soma Simples>)

// Agora, use atribuições normais para obter os valores do usuário.
num1 = inter(summon.x(<Digite o primeiro número:>))
num2 = inter(summon.x(<Digite o segundo número:>))

resultado = num1 + num2

pandora.expose(<O resultado da soma é: {resultado}>)